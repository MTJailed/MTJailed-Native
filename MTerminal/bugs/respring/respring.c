#include <stdio.h>
#include <stdlib.h>
#include "respring.h"

int respring() {
	
	//Open the respring payload from a webservice
	
	//Open the respring payload from a local file

	//The payload is not long enough to cause an overflow

	//A valid payload is received, start the overflow

	//Check if the application is still running
	return 0;
}
